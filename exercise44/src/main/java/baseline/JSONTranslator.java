package baseline;

import com.google.gson.JsonArray;

import java.util.ArrayList;

public class JSONTranslator {

    public ArrayList<Product> getProducts(String path) {
        // Create a JsonArray and call getJSONProductsFile(path).
        // Create an ArrayList of Product.
        // Create a loop the size of the JsonArray
            // For each object in the JsonArray, convert it to a product
            // Add that product to the ArrayList.
        // Return the ArrayList.
    }

    private JsonArray getJSONProductsFile(String path) {
        // Create a try catch method.
            // Create a JSON Parser.
            // Create a Json array and attach the parser to a filereader at the path.
        // If an error occurs print out that an error had occurred.
        // Return the Jsonarray.
    }
}
